// if a name, is a coaches name, log "it's a coach"
const name = "Sally";
var coachNames = ["John", "Alice", "Bob", "Sally"];
if (coachNames.includes(name)) {
    console.log("It's a coach!");
    }

// If a greeting, is bonjour, log "comment ca va "
const greeting = "bonjour";
if (greeting=="bonjour") {console.log("comment ca va?")};

//if a score reaches 10 log "you won the game"
let score = 10;
if(score >= 10) console.log("You won the game");

// if age, is greater than 65, "Time to retire
let age = 65
if(age>=65) {console.log("time to retire")};

// if hasCovid, log "time to isolate", else "keep social distancing"

let hasCovid = true;
if(hasCovid = true) {console.log ("Time to isolate")} else {console.log ("Keep social distancing")};

// if age, greater than 18, and isBritish, "you can vote in the next election"
const isBritish = true;
if (age > 17 && isBritish === true) {console.log("You can vote in the next election")} ;